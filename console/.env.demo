# Loaded by `vite --mode demo` (pnpm run dev:demo). Enables the in-memory mock
# client with fake SSE run playback — no orchestrator/cluster required.
VITE_DEMO=1
# Speed up the scripted run playback for demos/tests (higher = faster).
VITE_DEMO_SPEED=2
