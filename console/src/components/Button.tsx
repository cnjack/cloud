// The Button implementation moved to @jcloud/device-ui (M6 shared package) so
// the device views render identically in the console and the mobile app. This
// shim keeps every existing `components/Button` import working.
export { Button } from '@jcloud/device-ui';
