apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jcloud
resources:
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - bootstrap-rbac.yaml
  - bootstrap-configmap.yaml
  - bootstrap-job.yaml
