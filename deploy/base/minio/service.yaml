apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: jcloud
  labels:
    app.kubernetes.io/name: minio
    app.kubernetes.io/part-of: jcloud
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: minio
  ports:
    - name: s3
      port: 9000
      targetPort: 9000
    - name: console
      port: 9001
      targetPort: 9001
