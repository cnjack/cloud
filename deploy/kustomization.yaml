# Root kustomization so `kubectl apply -k deploy/` (or -k deploy/base/) both
# work. This MVP has no overlays (single target: local OrbStack), so it just
# points at base/.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - base
